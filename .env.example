# 在 https://console.x.ai 申请。也可在网页「设置」里填写（写入 data/settings.json，立即生效）。
XAI_API_KEY=

# 可选。默认 grok-4.6（支持 function calling + 视觉）
# 也可换成 grok-4、grok-4.5、grok-3 等
GROK_MODEL=grok-4.6

# 可选。图像生成模型
# GROK_IMAGE_MODEL=grok-imagine-image-2.0
